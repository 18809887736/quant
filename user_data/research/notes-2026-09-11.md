# 会话笔记 2026-09-11 — 项目状态与 AI 协作备忘

> 本文件是 Claude Code 与 Cursor 的共享记忆（按 CLAUDE.md 约定落盘）。
> 任何 AI 在新会话中读到本文件，即可对齐项目当前状态，无需重新探索。

## 项目当前状态

- 初始提交已完成：`321c83c`（8 个文件：骨架 + ResearchStub + 配置）
- remote：`github.com/18809887736/quant`。**2026-09-11 15:20 已转 private**（匿名访问 404；本机 git 凭据仍可 `ls-remote` 到 `d512711`）。`gh` 登录的 ebaneban888 不是协作者，CLI 看不到该仓——要用网页或所有者凭据操作。已推送至 `d512711`
- 路线图下一步（2026-09-11 15:41 对齐中，以 dialogue 为准）：
  1. 部署到海外 VPS，跑通 ResearchStub 首次回测（管道验证，与套利结论解耦）
  2. **不写** `funding-arb-plan.md`，直到全量费率矩阵 + 判定落盘
  3. BTC/ETH 常开正向资金费套利已用 2025-01 至今数据证伪（p90=0.01%，盖不住 0.35% 往返）
  4. 候选落地：观察器（默认）→ 负费率借币成本侦察（一天、不下单）→ 换方向（B 结案后再谈）

## 配置注意点（上 VPS 前要处理）

以下由 Cursor 于 2026-09-11 14:45 会话发现，均不阻塞 dry-run 回测：

1. **`.env` 未注入容器**：`docker-compose.yml` 没有把 `.env` 传给 freqtrade 容器；`config.json` 里 `key`/`secret` 为空字符串。公开行情下载和 dry-run 回测可直接跑；真要连交易所账户时再设计注入方式（密钥永不入库，硬规则 #2）
2. **FreqUI 默认凭证**：用户名/密码均为 `freqtrade`，`jwt_secret` 仍是 `CHANGE_ME_ON_VPS`。端口只监听回环地址；上 VPS 后必须改掉，且只经 SSH 隧道访问（`ssh -L 8080:127.0.0.1:8080 <vps>`）
3. **交易模式不匹配**：当前 `trading_mode: "spot"`，与资金费率套利（现货多 + 永续空）不是一套模式。这是已知架构边界——freqtrade 只做数据/回测/模拟盘基础设施，套利模块自研

## AI 协作通道（本机实测，2026-09-11）

| 方向 | 通道 | 状态 |
|------|------|------|
| Cursor → Claude Code | Cursor 全局 MCP 配了 `claude-code`（`claude.exe mcp serve`） | 可用：在 Cursor 里让 agent 调 claude-code 工具 |
| Claude Code → Cursor | 无 MCP、无 cursor-agent CLI | 只能读 `~/.cursor/projects/c-work-quant/agent-transcripts/` 下的会话记录，或用 `cursor -g <file>` 打开文件 |

**结论**：需要双向协作时，统一走本目录（`user_data/research/`）落盘——这是唯一被 CLAUDE.md 认可的跨会话记忆机制。每次会话结束前把结论追加到对应的 notes 文件。

## 本文件维护约定

- 新发现/新结论按日期追加新文件：`notes-YYYY-MM-DD.md`
- 过时信息直接改掉，不留"已失效"尾巴
- 交易相关的关键结论（回测结果、策略参数决定）单独成文件，不混进会话笔记
